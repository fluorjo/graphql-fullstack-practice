query Films($limit: Int, $cursor: Int) {
  films(limit: $limit, cursor: $cursor) {
    cursor
    films {
      id
      title
      subtitle
      runningTime
      release
      posterImg
      director {
        name
      }
      release
      posterImg
    }
  }
}
